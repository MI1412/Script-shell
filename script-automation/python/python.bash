echo "
 DOCS cmd bash

 install-pkg="pip install -r"
 py-version="python --version"
 venv="python -m venv"
 pyenv-install="pyenv install"
 pyenv-uninstall="pyenv uninstall"
 pyenv-global="pyenv global""
